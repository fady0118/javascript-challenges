### 2. Class + Inheritance Challenge

#### Create a base Character class and extend it:

##### Requirements

- Character has: name, level, stamina, and a rest(hours) method

- Make subclasses:
    - Warrior 
    - Mage 
    - Scout

Each subclass must implement a custom method (e.g. `powerStrike()`, `fireball()`, `recon()`).

Finally, create 3 instances and call their methods.